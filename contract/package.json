{
  "name": "obscura-contract",
  "version": "0.1.0",
  "private": true,
  "description": "Obscura Anchor programs — obscura_pool (commit-reveal). Escrow/settlement are interface-only.",
  "scripts": {
    "build": "anchor build",
    "test": "anchor test",
    "deploy:devnet": "anchor deploy --provider.cluster devnet"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.30.1",
    "@noble/hashes": "^1.7.1"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.7.3"
  }
}
